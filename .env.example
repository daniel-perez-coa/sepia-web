# In local development, put this value in .dev.vars (ignored by Git).
# In production configure the real stable user IDs; never use local_seedy.
ADMIN_USER_IDS=local_seedy
# Cloudflare Access email allowlist (comma-separated) for production.
ADMIN_USER_EMAILS=daniel.perez.coapango@gmail.com

# Local-only identity. In Cloudflare set this required secret to false.
LOCAL_DEV_AUTH=true
# Required Cloudflare Access allowlist for production (comma-separated).
ADMIN_USER_EMAILS=admin@example.com
# Optional stable Cloudflare Access subject IDs for production.
# ADMIN_USER_IDS=example-access-subject
